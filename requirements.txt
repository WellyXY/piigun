# Gateway (Railway)
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.0
redis[asyncio]>=5.0
asyncpg>=0.29
httpx>=0.27
boto3>=1.35

# Worker (RunPod) — heavy deps, not needed on Railway
# torch>=2.0
# diffusers>=0.30
# transformers>=4.40
# accelerate>=0.30
# Pillow>=10.0
# safetensors>=0.4
